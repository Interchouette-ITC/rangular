[build]
target = "index.html"
dist = "dist"
no_sri = true

[watch]
watch = [".", "../crates"]

[serve]
no_autoreload = false

[tools]
# Trunk 0.21 defaults to version_123; pin current Binaryen (matches host wasm-opt).
wasm_opt = "version_132"